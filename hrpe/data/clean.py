"""
Functions to help load raw data to work with
"""

import os
import glob
import pandas as pd


def clean_weather(data: pd.DataFrame):
    return data


def clean_hh_data(data: pd.DataFrame):
    return data


def clean_maxmin_data(data: pd.DataFrame):
    return data



def clean_minute_data(data: pd.DataFrame):
    return data
